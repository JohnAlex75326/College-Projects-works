/* program that accepts number input then adds 100 to the input 
*/

#include <stdio.h>
	int main(){
		int a;
		int b;
		printf("\n this program accepts a user input then adds 100 to it");			
		printf("\n enter an integer  ");
		scanf("%d",&a);
		
		b= a+100
		printf("\n the answer is = %d\n", b);
	}
